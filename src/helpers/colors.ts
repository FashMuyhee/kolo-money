import {Colors} from 'react-native-paper';

type Colors = {
  primary: string;
  white: string;
  bgDark: string;
  grey: string;
  accent: string;
};

const colors: Colors = {
  primary: '#0d60d8',
  white: '#ffffff',
  grey: Colors.grey600,
  bgDark: '#21254f',
  accent: '#BDC1F5',
};

export default colors;
